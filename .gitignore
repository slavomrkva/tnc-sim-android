node_modules/
android/app/build/
android/build/
android/.gradle/
android/local.properties
*.keystore
signing-key-info.txt
.getxfer*
github-recovery-codes.txt
desktop.ini

# Graphify creates full dated backups before rebuilding. Git history already
# preserves the previous tracked graph, so do not accumulate duplicate copies.
graphify-out/[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]/
