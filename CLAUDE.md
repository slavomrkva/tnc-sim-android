# TNC Sim Android

Read `NOTES.md` in this repo root before making any change — it has the
project map, non-obvious rules (applicationId, keystore signing, the correct
`origin` remote), and the release update flow.

After making a meaningful change, update `NOTES.md`: add a line to its
Changelog, and add a new numbered rule under "NON-OBVIOUS RULES" if you hit a
non-obvious pitfall.
